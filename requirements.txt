# Зависимости дашборда (рантайм). Версии зафиксированы вилкой:
# нижняя граница — на чём проверено, верхняя — до следующего мажора.
# Без верхней границы мажорный релиз Streamlit или pandas ломает живой деплой
# без единого коммита с нашей стороны (так уже случилось с use_container_width).
streamlit>=1.58,<2.0
duckdb>=1.5,<2.0
pandas>=2.3,<3.0
pyarrow>=15.0,<25.0
altair>=5.5,<7.0
