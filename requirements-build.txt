# Зависимости ТОЛЬКО для сборки данных/витрин (ETL и build-шаги).
# Для деплоя дашборда они НЕ нужны — там достаточно requirements.txt
# (streamlit / duckdb / pandas / pyarrow / altair).
#
# Установка окружения сборки:  pip install -r requirements-build.txt

requests>=2.31
pandas>=2.0
pyarrow>=15.0
duckdb>=1.0
openpyxl>=3.1
SQLAlchemy>=2.0
scikit-learn>=1.3      # только для scripts/build_player_segments.py (KMeans-сегментация)
pytest>=8.0            # юнит-тесты (tests/)
