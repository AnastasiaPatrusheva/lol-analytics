# Зависимости ТОЛЬКО для сборки данных/витрин (ETL и build-шаги).
# Для деплоя дашборда они НЕ нужны — там достаточно requirements.txt
# (streamlit / duckdb / pandas / pyarrow / altair).
#
# Версии зафиксированы вилкой: нижняя граница — на чём проверено,
# верхняя — до следующего мажора.
#
# Установка окружения сборки:  pip install -r requirements-build.txt

requests>=2.31,<3.0
pandas>=2.3,<3.0
pyarrow>=15.0,<25.0
duckdb>=1.5,<2.0
openpyxl>=3.1,<4.0
SQLAlchemy>=2.0,<3.0
scikit-learn>=1.3,<2.0   # только для scripts/build_player_segments.py (KMeans-сегментация)
pytest>=8.0,<10.0        # юнит-тесты (tests/)
