# --- Python ---
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# --- Node ---
node_modules/

# --- Инструменты / служебное (локально) ---
.claude/
archive/

# --- Большие / локальные / производные данные (НЕ в репозиторий) ---
# raw.zip (~437 МБ сырых матчей), archive.zip и т.п.
*.zip
*.db
# распакованный большой датасет (~553 МБ)
data/riot_full/
# исходный Kaggle xlsx (не редистрибутим)
data/raw/
# старый первый сэмпл
data/api_first_sample/
# сырьё Riot API (справочники data/reference/*.csv при этом остаются)
data/api/
# промежуточный слой (пересобирается скриптом)
data/normalized/
# датированные снимки витрин (версионирование, локально)
outputs/snapshots/
# производные выгрузки и локальная БД
outputs/warehouse/
outputs/data_quality/
outputs/api_eda/
outputs/common_eda/
# CSV-экспорты витрин (в репозитории храним только .parquet)
outputs/sql/*.csv
outputs/sql/star/*.csv

# --- Логи ---
*.log

# --- Секреты ---
.streamlit/secrets.toml
.env

# --- Личный материал (НЕ публиковать) ---
разбор_кода/
media/
docs/notes/
docs/task/
docs/transcripts/
notebooks/reference/
# внутренняя заметка-карта (в README есть раздел «Структура»)
КАРТА_ПРОЕКТА.md
# личные утилиты (расшифровка встреч, разовая уборка файлов) — не часть пайплайна
scripts/transcribe_meeting.py
scripts/inspect_transcription.py
scripts/organize_project_files.py
# выгрузки для Yandex DataLens (устарели: 2 источника, до riot_full)
datalens/

# ВАЖНО: outputs/sql/star/*.parquet НЕ игнорируем — это данные для дашборда.
# data/reference/*.csv тоже оставляем (маленькие справочники Data Dragon).
